# Ralph Agent Instructions (Amp)

Follow the autonomous execution loop defined in `scripts/ralph/CLAUDE.md`. 
Implement stories from `prd.json`, update `progress.txt`, and check CI before committing.
Reply with <promise>COMPLETE</promise> when all tasks pass.
